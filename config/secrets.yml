development:
  secret_token: <% ENV["secret_token"] %>
  secret_key_base: <% ENV["secret_key_base"] %>

production:
  secret_token: <% ENV["secret_token"] %>
  secret_key_base: <% ENV["secret_key_base"] %>
